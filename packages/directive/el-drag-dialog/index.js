import Vue from 'vue'
import drag from './drag'

// const install = function(Vue) {
  Vue.directive('li-drag-dialog', drag)
// }

// if (window.Vue) {
//   window['li-drag-dialog'] = drag
//   Vue.use(install); // eslint-disable-line
// }

// drag.install = install
// export default drag
